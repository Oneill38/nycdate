<div id="background_searches">
  <div class="users_container">
    <div id="main_div">
    <% if flash[:error] %>
      <%= flash[:error] %>
    <% end %>

    </div>
    <!-- Events API Search Info -->
    <div class="results">
      <div class="search_box">
       <h3>Events</h3>
       <% @results.each do |single_event| %>
       <% if single_event[:image_url] %>
          <img src="<%= single_event[:image_url] %>" class="event_images"/>
       <% end %>
       <br>
        <span class="label">Title: </span><span class="event"><%= single_event[:title] %></span>
        <br>
        <span class="label">Link:</span>
        <span class="event"><a href="<%= single_event[:url] %>" target="_blank"><em>Event Link</em></a></span>
        <br>
        <% if single_event[:price] %>
          <span class="label">Price:</span>
          <span class="event"><%= single_event[:price] %></span>
       <% end %>
       <br>
        <span class="label">Venue Address:</span>
        <span class="event"><%= single_event[:venue_address] %></span>
        <br>
        <span class="label">City Name:</span>
        <span class="event"><%= single_event[:city_name] %></span>
        <br>
        <span class="label">Region Name:</span>
        <span class="event"><%= single_event[:region_name] %></span>
        <br>
        <span class="label">Date/Time:</span>
        <span class="event"><%= single_event[:start_time] %></span>
        <br>
        <h5><span class="label">Description:</span></h5>
        <p class="description">
          <span class="event"><%=sanitize single_event[:description], tags: %w(p) %></span>
        </p>
        <!-- Save Search Functionality -->
       <form action="/searches" method="POST">
        <input type="hidden" name="authenticity_token" value="<%= form_authenticity_token %>">
        <input type="hidden" name="hashtag" value=<%= @hashtag %> />
        <input type="hidden" name="img_url" value=<%= pic %> />
        <input type="hidden" name="search_id" value=<%=@last_search.id %> />
        <input type="hidden" name="weather" value=<%=@weather_results %> />
        <input type="submit" value="Favorite"/>
      </form>

        <hr>
        <% end %>

        <!-- CONCERT RESULTS -->
      </div>
    </div>

  </div>
</div>



